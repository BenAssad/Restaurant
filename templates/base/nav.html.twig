<nav>
   <div id="menu">
        <div class="menu">
            <div class="logo">
                <a href="{{ path('app_home') }}"><img class="log" src="{{ asset('images/logo.png') }}" alt="logo"></a>
                <div id="imenu" aria-expanded="false">
                    <i data-feather="menu" id="bar3"></i>
                    <i data-feather="x" id="x"></i>
                </div>
            </div>
            <div id="menuri">
                <div class="right">
                    <a class="a" href="{{ path('app_home') }}">HOME</a>
                    <a class="a" href="{{ path('app_catalogue') }}">MENU</a>
                    <a class="a" href="{{ path('app_about') }}">ABOUT</a>
                    <a class="a" href="{{ path('app_book_table') }}">BOOK TABLE</a>
                </div>
            </div>
            <div class="pro">
                <div class="profile">
                    {% if app.user %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('app_admin_produit_index') }}" class="btnd">Back Office</a>
                            <a href="{{ path('app_admin_table') }}" class="btnd">Reservation</a>
                        {% endif %}
                            <a class="btnd" href="{{ path('app_logout') }}">Se Déconnecter</a>
                        {% if app.user.avatar %}
                            <a href="{{ path('app_profile') }}"><img id="avatar" src="{{ asset('images/avatar/' ~ app.user.avatar) }}" alt="avatar" class="avatar" aria-expanded="false"></a>
                        {% else %}
                            <img id="avatar" src="{{ asset('images/avatar.png') }}" alt="avatar" aria-expanded="false">
                        {% endif %}

                    {% else %}
                        <div class="seconnecter">
                            <a class="btn" href="{{ path('app_register') }}">S'Inscrire</a>
                            <a class="btn" href="{{ path('app_login') }}">Se Connecter</a>
                        </div>
                    {% endif %}
                        <a href="{{ path('app_cart') }}"><i data-feather="shopping-cart" class="cart1"></i></a>
                </div>
            </div>
        </div>
        
    </div>
</nav>

